- if !searchterm
  - searchterm = ''

- if show_entry?(item)
  .item_listing
    .row.discussion
      .small-12.columns.title
        = link_to raw(item.name), item
      
    = render :partial => 'shared/discussion_entry', locals: {chat: item , master: master.nil? ? false : master}
